{
  "name": "cv-auto-apply-extension",
  "version": "0.1.0",
  "description": "cv-auto-apply Chrome extension - smart auto-fill for job applications",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
